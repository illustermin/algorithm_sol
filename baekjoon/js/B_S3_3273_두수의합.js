function solution(numbers){
  //빈 배열 생성
    const ret=[];
  // 두 수를 선택하는 모든 경우의 수
    for (let i = 0; i < numbers.length; i++) {
        const element = array[i];
        
    }
  // 두 수를 더한 결과를 새로운 배열에 추가

  //중복된 값 제거, 오름차순 정렬
} 